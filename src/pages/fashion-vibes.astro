---
// Unlisted, noindex EPK variant shared privately with fashion and luxury
// clients only. Content sourced verbatim from the private Gamma page at
// epk-fl-maya-fourteen-6m0uheh.gamma.site (text and photos re-hosted here,
// see src/data/v3/images.js's "fashion*" keys), with two mandated copy
// edits in the Award-Winning Artist section (see inline comments below).
// Structurally modeled on epk.astro (direct V3BaseLayout usage, not
// V3InteriorLayout, so it can carry image+text sections and card grids),
// but with its own "fv-" prefixed styles rather than epk's "epk-" ones,
// and a CSS-only fixed starfield background layer instead of a hero photo
// background — see the .fv-starfield rule for why position:fixed (not
// background-attachment:fixed, which is broken on iOS Safari) is required.
//
// Deliberately unreachable from any nav: not present in navItems.js (the
// hamburger menu's own list), not linked from any other page. Reachable
// only by direct URL. See astro.config.mjs's sitemap filter for the
// matching sitemap exclusion.
//
// Visual direction (2026-07-19, explicit instruction): closely matches the
// Gamma reference's own layout and colors section-by-section — orange
// accent (--accent, sampled from the reference), card fill/border/radius,
// two-column hero, per-section heading colors, and the Booking section's
// flatter inline-link structure. The one deliberate exception is
// typography: this page keeps the site's serif (Cormorant Garamond), never
// Gamma's sans. This supersedes an earlier pass that used the homepage's
// gold accent and kept everything monochrome — don't revert to that
// without a new explicit instruction.
//
// Round 2 (2026-07-19, side-by-side review vs. the Gamma reference):
// lighter/more translucent glass cards so the starfield stays visible
// while scrolling; no forced crops on artwork/covers (OCEANUS, the Sync
// Beat magazine spread — see .fv-media-frame--natural); larger hero
// portrait; Luxury Hospitality reverted to the original Bali portrait +
// copy; Sound & Atmosphere restructured to three small clickable tiles in
// a row (matching the Gamma reference) with the descriptive paragraph
// below instead of above; Press Assets paragraph moved below the photo
// grid and its buttons left-aligned on desktop; Discover MAYA's colored
// badge/line/brand-color pattern dropped for a plain monochrome icon row
// (also gained a Website entry, first); beepd logo removed from Booking.
import V3BaseLayout from '../layouts/V3BaseLayout.astro';
import HamburgerMenu from '../components/v3/HamburgerMenu.astro';
import TextFooter from '../components/v3/TextFooter.astro';
import { images } from '../data/v3/images.js';

// Local to this page, not socialLinks.js: this exact four-item set
// (including LinkedIn, which isn't part of the site's standard five-icon
// row) is specific to the brief for this page only — adding LinkedIn to
// the shared file would silently grow every other page's Discover/nav row.
// `color` values are sampled directly from the Gamma reference (badge
// border + label color per platform), not invented.
const fashionDiscoverLinks = [
  { href: 'https://www.instagram.com/mayafourteen/', title: 'Instagram', type: 'fa', name: 'instagram', color: '#B05EF1' },
  { href: 'https://www.youtube.com/@MayaFourteen', title: 'YouTube', type: 'fa', name: 'youtube', color: '#F44444' },
  { href: 'https://soundcloud.com/MayaFourteen', title: 'SoundCloud', type: 'fa', name: 'soundcloud', color: '#FFA44F' },
  { href: 'https://www.linkedin.com/in/mayafourteen/', title: 'LinkedIn', type: 'fa', name: 'linkedin', color: '#5E98F1' },
];

const appearances = [
  {
    title: 'Formula 1 Circuits',
    copy: 'Private after-race performances during Monaco and Abu Dhabi Grand Prix weekends.',
    image: images.fashionF1,
    alt: 'Maya Fourteen at a Formula 1 after-race event',
  },
  {
    title: 'Film Festivals',
    copy: 'Music curation and private performance for select soirées during the Cannes Film Festival.',
    image: images.fashionFilm,
    alt: 'Maya Fourteen at the Cannes Film Festival',
  },
  {
    title: 'Luxury Hospitality',
    copy: 'Live performance at W Budapest and a curated destination-set recorded in Bali, blending atmosphere, rhythm, and a strong sense of place.',
    image: images.fashionHospitality,
    alt: 'Maya Fourteen performing at W Budapest',
  },
  {
    title: 'Brand Activations',
    copy: 'Exclusive private performance for FENDI & GT Home at their Pininfarina YachtHouse unit launch.',
    image: images.fashionBrand,
    alt: 'Maya Fourteen at the FENDI & GT Home Pininfarina YachtHouse launch',
  },
];

// SOUND & ATMOSPHERE: click-to-load facades, not eager iframes — the
// YouTube + two SoundCloud widgets each pull in third-party JS that's
// unnecessary until a visitor actually wants to listen/watch, so keeping
// them out of the initial load protects LCP/CWV. SoundCloud's oEmbed API
// is called at BUILD time (this runs once during `astro build`, not per
// visitor) to get each track's real artwork for the facade poster —
// falls back to no poster (still a functional dark tile with a play
// button) if the network call fails, rather than breaking the build.
const YOUTUBE_VIDEO_ID = 'lcOJEm9-DjI';

const soundcloudTracks = [
  {
    url: 'https://soundcloud.com/mayafourteen/an-organic-deep-tech-storytelling-set-with-magical-vibes-from-nusa-dua-bali',
    embedSrc: 'https://w.soundcloud.com/player/?url=https%3A//soundcloud.com/mayafourteen/an-organic-deep-tech-storytelling-set-with-magical-vibes-from-nusa-dua-bali&color=%23141414&auto_play=true&hide_related=true&show_comments=false&show_user=true&show_reposts=false&show_teaser=false&visual=true',
    title: 'Maya Fourteen: An Organic Deep Tech Storytelling Set with Magical Vibes from Nusa Dua, Bali',
  },
  {
    url: 'https://soundcloud.com/mayafourteen/maya-etnika-tajbc',
    embedSrc: 'https://w.soundcloud.com/player/?url=https%3A//soundcloud.com/mayafourteen/maya-etnika-tajbc&color=%23141414&auto_play=true&hide_related=true&show_comments=false&show_user=true&show_reposts=false&show_teaser=false&visual=true',
    title: 'Maya Fourteen: Etnika TAJBC',
  },
];

async function fetchSoundCloudThumb(trackUrl: string): Promise<string | null> {
  try {
    const res = await fetch(`https://soundcloud.com/oembed?url=${encodeURIComponent(trackUrl)}&format=json`);
    if (!res.ok) return null;
    const data = await res.json();
    return data.thumbnail_url ?? null;
  } catch {
    return null;
  }
}

const soundcloudThumbs = await Promise.all(soundcloudTracks.map((t) => fetchSoundCloudThumb(t.url)));
---

<V3BaseLayout
  title="MAYA FOURTEEN | Fashion & Luxury Vibes EPK"
  description="Private electronic press kit for fashion and luxury clients: Maya Fourteen's runway sound curation, brand activations and international bookings."
  bodyClass="page-fashion-vibes"
  lang="en"
>
  <Fragment slot="head">
    <meta name="robots" content="noindex, nofollow" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preconnect" href="https://cdnjs.cloudflare.com">
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;1,300&family=Inter:wght@200;300;400;500&display=swap" />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;1,300&family=Inter:wght@200;300;400;500&display=swap" rel="stylesheet" media="print" onload="this.media='all'">
    <noscript><link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;1,300&family=Inter:wght@200;300;400;500&display=swap" rel="stylesheet"></noscript>
    <link rel="preload" as="style" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/fontawesome.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/fontawesome.min.css" media="print" onload="this.media='all'">
    <noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/fontawesome.min.css"></noscript>
    <link rel="preload" as="style" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/brands.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/brands.min.css" media="print" onload="this.media='all'">
    <noscript><link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/brands.min.css"></noscript>
  </Fragment>

  <!-- Fixed, non-scrolling starfield layer. position:fixed (not
       background-attachment:fixed, which iOS Safari doesn't render
       correctly) so it stays pinned behind the page while .fv-content
       scrolls above it. -->
  <div class="fv-starfield" aria-hidden="true"></div>

  <HamburgerMenu lang="en" />

  <main class="fv">
    <!-- HERO: same card shell as every other section (Gamma's hero lives
         inside the identical bordered card, not a standalone treatment),
         two columns, portrait left. -->
    <section class="fv-section fv-hero fade-item">
      <div class="fv-hero-portrait-frame">
        <img
          class="fv-hero-portrait"
          src={images.fashionHero.src}
          width={images.fashionHero.width}
          height={images.fashionHero.height}
          alt="Maya Fourteen"
          loading="eager"
          fetchpriority="high"
        />
      </div>
      <div class="fv-hero-content">
        <h1 class="fv-name">MAYA FOURTEEN</h1>
        <p class="fv-tagline">Electronic Music Artist &amp; Cultural Curator</p>
        <p class="fv-eyebrow">[ Fashion &amp; Luxury Vibes EPK ]</p>
      </div>
    </section>

    <!-- ARTIST PROFILE -->
    <section class="fv-section fv-media fv-media-reverse fade-item">
      <div class="fv-media-frame">
        <img
          class="fv-media-image"
          src={images.fashionProfile.src}
          width={images.fashionProfile.width}
          height={images.fashionProfile.height}
          alt="Maya Fourteen behind the decks"
          loading="lazy"
        />
      </div>
      <div class="fv-media-text">
        <h2 class="fv-heading-accent">Artist Profile</h2>
        <p>
          MAYA is a Brazilian electronic music artist known for her versatility across both
          clubbing environments and refined, design-driven settings. With a highly developed
          musical taste and extensive repertoire, her sound balances atmosphere, rhythm, and
          emotional flow, adapting naturally from dancefloor performances to sophisticated
          brand and cultural contexts.
        </p>
        <p>
          Based between Brazil and Europe, MAYA is booked for clubs, festivals, and curated
          events, as well as bespoke musical selections for private, luxury, and hospitality
          environments. Acting as a sonic curator, she delivers performances shaped by timing
          and spatial awareness, whether driving the room or subtly elevating it.
        </p>
      </div>
    </section>

    <!-- AWARD-WINNING ARTIST -->
    <section class="fv-section fv-media fade-item">
      <div class="fv-media-frame">
        <img
          class="fv-media-image"
          src={images.fashionAward.src}
          width={images.fashionAward.width}
          height={images.fashionAward.height}
          alt="Maya Fourteen at IMS Ibiza 2025"
          loading="lazy"
        />
      </div>
      <div class="fv-media-text">
        <h2 class="fv-heading-accent">Award-Winning Artist</h2>
        <!--
          Two mandated copy edits from the source Gamma page, per brief:
          (a) the IMS clause must read "a Future Talent Award at IMS Ibiza
              2025, selected by Pete Tong" (was: "...IMS Ibiza 2025, one of
              the most respected platforms in global electronic music
              culture").
          (b) "she was also granted a special distinction" is replaced with
              "she was also awarded the Nicole Moudaber Talent Highlight
              Prize, a personal distinction from Nicole Moudaber".
          No em dashes anywhere on this page, commas only.
        -->
        <p>
          MAYA has received international recognition for her work, including a Future Talent
          Award at IMS Ibiza 2025, selected by Pete Tong. In the same edition, she was also
          awarded the Nicole Moudaber Talent Highlight Prize, a personal distinction from
          Nicole Moudaber, highlighting her artistic clarity and positioning her among the
          most forward-looking artists of her generation.
        </p>
        <p>
          In 2023, she was selected as part of a highly curated group of artists at the
          MixMasters Ibiza Get Signed Summit, chosen from thousands of participants worldwide.
          This early recognition reflected endorsement from industry leaders and acknowledged
          her artistic potential.
        </p>
      </div>
    </section>

    <!-- SELECTED GLOBAL APPEARANCES -->
    <section class="fv-section fade-item">
      <h2>Selected Global Appearances</h2>
      <div class="fv-appearance-grid">
        {appearances.map((item) => (
          <div class="fv-appearance-card">
            <div class="fv-appearance-image-frame">
              <img
                class="fv-appearance-image"
                src={item.image.src}
                width={item.image.width}
                height={item.image.height}
                alt={item.alt}
                loading="lazy"
              />
            </div>
            <div class="fv-appearance-text">
              <h3>{item.title}</h3>
              <p>{item.copy}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <!-- FASHION SOUND CURATION: OCEANUS cover art is a native 1:1 square
         (see fashionOceanus1 in images.js) — the default .fv-media-frame's
         forced 3:4 crop was slicing off the "O" and the "MAYA" logo, so
         this frame uses the natural-ratio modifier instead (see
         .fv-media-frame--natural below). -->
    <section class="fv-section fv-media fade-item">
      <div class="fv-media-frame fv-media-frame--natural" style="aspect-ratio: 1 / 1;">
        <img
          class="fv-media-image fv-media-image--natural"
          src={images.fashionOceanus1.src}
          width={images.fashionOceanus1.width}
          height={images.fashionOceanus1.height}
          alt="Maya Fourteen, OCEANUS runway soundtrack curation"
          loading="lazy"
        />
      </div>
      <div class="fv-media-text">
        <h2>Fashion Sound Curation</h2>
        <p>
          MAYA curated the official runway soundtrack for OCEANUS during Paris and London
          Fashion Weeks, developing a bespoke sonic narrative aligned with the brand's
          retro-futuristic visual identity.
        </p>
        <p>
          Drawing from 80s-inspired synth textures and contemporary electronic design, the
          soundtrack was crafted to support runway pacing, silhouette movement, and
          atmosphere. The result is a refined, forward-facing soundscape that balances
          nostalgia and modernity, enhancing the rhythm and emotional flow of the show without
          overpowering it.
        </p>
        <a class="fv-inline-link" href="https://www.instagram.com/p/DQkSW7aESiR/" target="_blank" rel="noopener">View the OCEANUS runway soundtrack &rarr;</a>
      </div>
    </section>

    <!-- MEDIA SPOTLIGHT -->
    <section class="fv-section fv-media fade-item">
      <div class="fv-media-frame">
        <img
          class="fv-media-image"
          src={images.fashionOceanus2.src}
          width={images.fashionOceanus2.width}
          height={images.fashionOceanus2.height}
          alt="Maya Fourteen, OCEANUS runway soundtrack curation"
          loading="lazy"
        />
      </div>
      <div class="fv-media-text">
        <h2>Media Spotlight</h2>
        <p>
          MAYA was featured on the cover of Spain's Sync Beat Magazine, with the editorial
          photographed during a private presentation for FENDI &amp; GT Home at the Pininfarina
          YachtHouse, one of Balneário Camboriú's most iconic luxury developments. The
          feature places her within curated, high-end environments where music, fashion, and
          design converge naturally, reinforcing her positioning beyond traditional club
          contexts.
        </p>
      </div>
    </section>

    <!-- SOUND & ATMOSPHERE -->
    <section class="fv-section fade-item">
      <h2>Sound &amp; Atmosphere</h2>
      <p class="fv-instruction">Explore selected visual and audio projects by clicking the images below.</p>
      <p>
        MAYA's work extends beyond clubs, radios and festivals, into carefully selected
        audiovisual projects and cultural settings worldwide. Alongside her club and festival
        activity, she has developed bespoke sets and sound narratives for fashion, private
        events, and brand-driven environments, adapting her musical language to context
        without compromising identity.
      </p>
      <div class="fv-embed-frame fv-embed-video">
        <button
          type="button"
          class="fv-embed-facade"
          data-embed-type="video"
          data-embed-src={`https://www.youtube.com/embed/${YOUTUBE_VIDEO_ID}?autoplay=1`}
          data-embed-title="Maya Fourteen, video set"
          aria-label="Play video: Maya Fourteen, video set"
        >
          <img src={`https://img.youtube.com/vi/${YOUTUBE_VIDEO_ID}/maxresdefault.jpg`} alt="" loading="lazy" width="1280" height="720" />
          <span class="fv-embed-play" aria-hidden="true"></span>
        </button>
      </div>
      {soundcloudTracks.map((track, i) => (
        <button
          type="button"
          class="fv-embed-facade fv-embed-facade-audio"
          data-embed-type="audio"
          data-embed-src={track.embedSrc}
          data-embed-title={track.title}
          data-embed-height="400"
          aria-label={`Play track: ${track.title}`}
        >
          {soundcloudThumbs[i] && <img src={soundcloudThumbs[i]!} alt="" loading="lazy" width="500" height="500" />}
          <span class="fv-embed-play" aria-hidden="true"></span>
        </button>
      ))}
    </section>

    <!-- PRESS ASSETS -->
    <section class="fv-section fade-item">
      <h2>Press Assets</h2>
      <p>High-resolution images available for promotional use. All photos copyright protected.</p>
      <div class="fv-photo-grid">
        <div class="fv-photo-frame"><img src={images.fashionPress1.src} width={images.fashionPress1.width} height={images.fashionPress1.height} alt="Maya Fourteen, editorial portrait in a black textured outfit" loading="lazy" /></div>
        <div class="fv-photo-frame"><img src={images.fashionPress2.src} width={images.fashionPress2.width} height={images.fashionPress2.height} alt="Maya Fourteen, portrait seated on rustic wooden steps" loading="lazy" /></div>
        <div class="fv-photo-frame"><img src={images.fashionPress3.src} width={images.fashionPress3.width} height={images.fashionPress3.height} alt="Maya Fourteen, portrait against a corrugated metal wall" loading="lazy" /></div>
        <div class="fv-photo-frame"><img src={images.fashionPress4.src} width={images.fashionPress4.width} height={images.fashionPress4.height} alt="Maya Fourteen, portrait in the desert at golden hour" loading="lazy" /></div>
      </div>
      <div class="fv-download-actions">
        <a class="cta-button cta-button-solid" href="https://drive.google.com/open?id=1Y0dyWs18uc7w8QbKFj_pbmrpeX81bu_j" target="_blank" rel="noopener">Download Press Kit</a>
        <a class="cta-button cta-button-outline" href="https://drive.google.com/open?id=1XacZsWwys1ieA5C3hLZzSoFWPFS_yh2z" target="_blank" rel="noopener">Download Full Artist EPK</a>
      </div>
    </section>

    <!-- DISCOVER MAYA: horizontal line with each platform's icon in a
         rounded-square badge sitting on it, a short drop line, and a
         brand-colored label beneath — matches the Gamma reference's
         structure exactly. Per-link accent color is a CSS custom property
         set inline (one value per link, cheaper than four near-duplicate
         CSS rules). -->
    <section class="fv-section fv-discover fade-item">
      <h2>Discover MAYA</h2>
      <div class="fv-discover-row">
        <div class="fv-discover-line" aria-hidden="true"></div>
        {fashionDiscoverLinks.map((link) => (
          <a
            class="fv-discover-item"
            href={link.href}
            target="_blank"
            rel="noopener"
            title={link.title}
            aria-label={`${link.title} (opens in a new tab)`}
            style={`--brand-color: ${link.color}`}
          >
            <span class="fv-discover-badge">
              <i class={`fa-brands fa-${link.name}`} aria-hidden="true"></i>
            </span>
            <span class="fv-discover-drop" aria-hidden="true"></span>
            <span class="fv-discover-label">{link.title}</span>
          </a>
        ))}
      </div>
    </section>

    <!-- BOOKING & MANAGEMENT: left-aligned contact block inside a subtle
         inner card, plain inline links (email white, WhatsApp orange) not
         buttons, beepd logo centered in its own footer strip — matches
         the Gamma reference's actual structure, which is flatter than a
         button-CTA pattern. -->
    <section class="fv-section fv-booking fade-item">
      <h2>Booking &amp; Management</h2>
      <div class="fv-booking-card">
        <div class="fv-booking-content">
          <p class="fv-booking-heading">For Bookings &amp; Artist Enquiries</p>
          <p class="fv-booking-name">Antonio Afonso, Management</p>
          <p class="fv-booking-contact">
            Email: <a class="fv-booking-link" href="mailto:hello@mayafourteen.com">hello@mayafourteen.com</a>
          </p>
          <p class="fv-booking-contact">
            <span aria-hidden="true">&#128222;</span> +55 47 99935-3919
            <span aria-hidden="true">&nbsp;I&nbsp;</span>
            <a class="fv-booking-link fv-booking-link-accent" href="https://wa.me/5547999353919" target="_blank" rel="noopener">WhatsApp</a>
          </p>
          <p class="fv-booking-note">
            Available for select international appearances, including curated events, cultural
            programming, and brand collaborations. Club and festival bookings considered on a
            case-by-case basis. Full professional technical rider available upon request.
          </p>
        </div>
        <div class="fv-booking-footer">
          <img src="/images/fashion/beepd-logo.svg" alt="beepd | MUSIC" width="106" height="27" />
        </div>
      </div>
    </section>
  </main>

  <TextFooter />

  <script>
    // Click-to-load facades (Sound & Atmosphere): the real YouTube/
    // SoundCloud iframe is only created once a visitor actually clicks —
    // keeps third-party embed JS out of the initial page load entirely.
    document.querySelectorAll<HTMLButtonElement>('.fv-embed-facade').forEach((btn) => {
      btn.addEventListener('click', () => {
        const src = btn.dataset.embedSrc;
        if (!src) return;
        const iframe = document.createElement('iframe');
        iframe.src = src;
        iframe.title = btn.dataset.embedTitle ?? '';
        if (btn.dataset.embedType === 'video') {
          iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';
          iframe.allowFullscreen = true;
          iframe.className = 'fv-embed-injected fv-embed-injected-video';
        } else {
          iframe.allow = 'autoplay';
          iframe.scrolling = 'no';
          iframe.height = btn.dataset.embedHeight ?? '400';
          iframe.className = 'fv-embed-injected fv-embed-injected-audio';
        }
        btn.replaceWith(iframe);
      });
    });
  </script>

  <script>
    // Scroll-triggered entrance: every .fade-item starts hidden (see the
    // matching CSS) and gets .is-visible added the first time it enters
    // the viewport, then is unobserved so it never replays. Respects
    // prefers-reduced-motion by skipping the animation entirely — content
    // is just present from the start for those users, see the CSS guard.
    const revealEls = document.querySelectorAll<HTMLElement>('.fade-item');
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    if (prefersReducedMotion) {
      revealEls.forEach((el) => el.classList.add('is-visible'));
    } else if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
              observer.unobserve(entry.target);
            }
          });
        },
        { threshold: 0.15, rootMargin: '0px 0px -40px 0px' }
      );
      revealEls.forEach((el) => observer.observe(el));
    } else {
      revealEls.forEach((el) => el.classList.add('is-visible'));
    }
  </script>
</V3BaseLayout>

<style is:global>
  /* Scroll-triggered entrance, not a load-time animation: every element
     starts hidden and gets .is-visible added by the IntersectionObserver
     script below the first time it scrolls into view (once — the
     observer unobserves after triggering), so each section reveals as
     the visitor actually reaches it rather than all firing on page load.
     One pattern, one duration, one easing, site-wide on this page. */
  .fade-item {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
  }

  .fade-item.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  @media (prefers-reduced-motion: reduce) {
    .fade-item {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<style>
  :global(.page-fashion-vibes) {
    color: #fff;
    font-family: 'Inter', sans-serif;
    background: #050506;
  }

  /* No language switcher on this private page (see file header comment) —
     hidden per-page via bodyClass rather than touching LanguageMenu itself,
     which every other V3 page still needs unmodified. */
  :global(.page-fashion-vibes) :global(.language-menu) {
    display: none;
  }

  :global(.page-fashion-vibes) :global(.language-menu-trigger) {
    font-size: 0.95rem;
    letter-spacing: 0.12em;
    padding: 10px 14px 10px 10px;
    color: rgba(255, 255, 255, 0.9);
  }

  /* FIXED STARFIELD BACKGROUND
     position: fixed (never background-attachment: fixed, which iOS Safari
     fails to render correctly) keeps this pinned behind the viewport while
     .fv scrolls above it, so stars stay visible through and between the
     translucent cards. Pure CSS, three layered radial-gradient dot fields
     at different densities/sizes for a parallax-like depth read, plus a
     faint white nebula wash, all monochrome, no new colors. */
  .fv-starfield {
    position: fixed;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    background: #050506;
  }

  .fv-starfield::before,
  .fv-starfield::after {
    content: '';
    position: absolute;
    inset: -20%;
    background-repeat: repeat;
  }

  .fv-starfield::before {
    background-image:
      radial-gradient(1.6px 1.6px at 10% 20%, rgba(255, 255, 255, 0.9), transparent 100%),
      radial-gradient(1.2px 1.2px at 35% 65%, rgba(255, 255, 255, 0.65), transparent 100%),
      radial-gradient(1.8px 1.8px at 65% 15%, rgba(255, 255, 255, 0.85), transparent 100%),
      radial-gradient(1px 1px at 85% 75%, rgba(255, 255, 255, 0.55), transparent 100%),
      radial-gradient(1.4px 1.4px at 20% 85%, rgba(255, 255, 255, 0.7), transparent 100%),
      radial-gradient(1.2px 1.2px at 55% 45%, rgba(255, 255, 255, 0.6), transparent 100%);
    background-size: 340px 340px;
  }

  .fv-starfield::after {
    background-image:
      radial-gradient(1px 1px at 6% 45%, rgba(255, 255, 255, 0.5), transparent 100%),
      radial-gradient(1px 1px at 48% 10%, rgba(255, 255, 255, 0.4), transparent 100%),
      radial-gradient(1.3px 1.3px at 28% 70%, rgba(255, 255, 255, 0.55), transparent 100%),
      radial-gradient(1px 1px at 78% 40%, rgba(255, 255, 255, 0.4), transparent 100%),
      radial-gradient(1.1px 1.1px at 92% 88%, rgba(255, 255, 255, 0.5), transparent 100%);
    background-size: 220px 220px;
    opacity: 0.7;
  }

  /* Vertical rhythm matches the reference's own generous ~64px gap
     between cards (measured directly on the Gamma page at this same
     1440px width), a big jump from a tighter stack. */
  .fv {
    position: relative;
    z-index: 1;
    max-width: 1080px;
    margin: 0 auto;
    padding: 140px 4% 100px;
    display: flex;
    flex-direction: column;
    gap: 56px;
  }

  /* Accent: Gamma's exact orange, sampled from the reference (badge
     border/label color for Instagram/YouTube/SoundCloud/LinkedIn is its
     own separate --brand-color per link, set inline — see .fv-discover
     below). Applied where Gamma applies it: the EPK eyebrow tag, the
     Artist Profile / Award-Winning Artist headings (.fv-heading-accent),
     the appearance-card labels, the Sound & Atmosphere instruction line,
     the WhatsApp link, and the buttons. Every other section heading stays
     white, matching the reference's own per-section mix. */
  :global(.page-fashion-vibes) {
    --accent: #fc8337;
  }

  /* HERO: two columns inside the same card shell as every other section
     (see .fv-section) — portrait left, wordmark/tagline/eyebrow right,
     matching the Gamma reference's layout exactly. */
  .fv-hero {
    display: grid;
    grid-template-columns: minmax(220px, 380px) 1fr;
    gap: 48px;
    align-items: center;
  }

  .fv-hero-content {
    display: flex;
    flex-direction: column;
  }

  .fv-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(2.2rem, 5vw, 3.6rem);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-weight: 300;
    margin: 0 0 12px 0;
  }

  .fv-tagline {
    font-size: clamp(1.05rem, 2vw, 1.3rem);
    font-weight: 300;
    font-style: italic;
    color: rgba(255, 255, 255, 0.75);
    margin: 0 0 16px 0;
  }

  .fv-eyebrow {
    font-size: 0.75rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: var(--accent);
    margin: 0;
  }

  /* Intentional, responsive container instead of a bare <img> — width is
     never left to intrinsic sizing alone, so the portrait can't get
     clipped by an ancestor at any viewport. object-fit: contain (not
     cover) guarantees the full cutout figure is always visible even if
     this asset's aspect ratio ever changes; for the current 1:1 asset in
     this 1:1 frame the two behave identically, contain is just the safer
     default going forward. No radius, no crop — matches the reference,
     which shows this same cutout uncropped and square. */
  .fv-hero-portrait-frame {
    width: 100%;
    aspect-ratio: 1 / 1;
  }

  .fv-hero-portrait {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
  }

  /* SECTION SHELL: round 2 (2026-07-19) lowered the fill to rgba(10,10,12,0.4)
     with a lighter blur, per direct client feedback that the previous
     0.75-opacity fill read as near-opaque black and hid the starfield while
     scrolling — the whole point of the fixed starfield layer above. Border
     and radius unchanged. Contrast check: the underlying starfield is #050506
     with only sparse 1-2px star points, so blending 40% of a near-black
     overlay over it keeps effective background luminance low almost
     everywhere; body copy (rgba(255,255,255,0.8) on this) and the orange
     accent both clear WCAG AA (4.5:1) against that dark blend. */
  .fv-section {
    background: rgba(10, 10, 12, 0.4);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(78, 78, 78, 0.6);
    border-radius: 22px;
    padding: 44px;
  }

  .fv-section h2 {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(1.6rem, 3vw, 2.2rem);
    font-weight: 300;
    letter-spacing: 0.04em;
    margin: 0 0 20px 0;
  }

  /* Orange section headings: Gamma colors only Artist Profile and
     Award-Winning Artist this way, every other section heading stays
     white (the default) — matches the reference's own mix exactly, not
     a blanket "all headings orange" rule. */
  .fv-heading-accent {
    color: var(--accent);
  }

  /* Sound & Atmosphere's orange instruction line, verbatim from the
     reference. Still accurate here: the click-to-load facades (see
     .fv-embed-facade) are literally images a visitor clicks to load the
     real player, so the copy still describes the real interaction. */
  .fv-instruction {
    color: var(--accent) !important;
  }

  /* Line-length cap: sections with no image column (Sound & Atmosphere,
     Press Assets, the appearances intro) span the section's full width,
     which at this font size runs well past a comfortable reading
     measure. Media-section paragraphs are already narrower than 70ch
     from the grid column itself, so this is a no-op there. */
  .fv-section p {
    font-weight: 200;
    line-height: 1.8;
    letter-spacing: 0.02em;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0 0 16px 0;
    max-width: 70ch;
  }

  .fv-section p:last-of-type {
    margin-bottom: 0;
  }

  /* MEDIA (image + text) */
  .fv-media {
    display: grid;
    grid-template-columns: 340px 1fr;
    gap: 40px;
    align-items: center;
  }

  .fv-media-reverse {
    grid-template-columns: 1fr 340px;
  }

  .fv-media-reverse .fv-media-frame {
    order: 2;
  }

  /* aspect-ratio lives on this wrapping frame, not the <img> itself — see
     the matching comment on .fv-appearance-image-frame below for why.
     3:4 is the default crop for straight photography (Artist Profile,
     Award-Winning Artist). Fashion Sound Curation and Media Spotlight show
     actual artwork/cover images instead, which must never be cropped —
     see .fv-media-frame--natural below, which those two sections use in
     place of this default. */
  .fv-media-frame {
    aspect-ratio: 3 / 4;
    overflow: hidden;
    border-radius: 16px;
  }

  .fv-media-image {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* NO-CROP MODIFIER: for artwork/cover images that must show in full
     (OCEANUS cover art, the Sync Beat magazine spread) — aspect-ratio is
     set inline per instance (each asset's own real ratio, not the shared
     3:4 default) and object-fit: contain guarantees nothing is sliced off,
     unlike the cover-crop default above. */
  .fv-media-frame--natural {
    background: rgba(0, 0, 0, 0.2);
  }

  .fv-media-image--natural {
    object-fit: contain;
  }

  .fv-media .fv-inline-link {
    display: inline-block;
    margin-top: 4px;
  }

  .fv-inline-link {
    color: var(--accent);
    text-decoration: none;
    border-bottom: 1px dotted rgba(252, 131, 55, 0.5);
    font-size: 0.85rem;
    letter-spacing: 0.03em;
    transition: color 0.3s ease, border-color 0.3s ease;
  }

  .fv-inline-link:hover,
  .fv-inline-link:focus-visible {
    color: #fff;
    border-bottom-style: solid;
    border-bottom-color: rgba(255, 255, 255, 0.85);
  }

  /* SELECTED GLOBAL APPEARANCES: 4 equal columns at desktop (see the
     mobile query below for the 2x2 collapse). Each card is just an image
     frame + caption sitting directly on the section's own glass fill, no
     separate nested card border, matching the reference layout. */
  .fv-appearance-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 18px;
  }

  .fv-appearance-card {
    display: flex;
    flex-direction: column;
  }

  /* aspect-ratio lives on the wrapping frame, not the <img> itself: an
     <img> with both an explicit CSS aspect-ratio and HTML width/height
     attributes inside a CSS grid track resolves its height from the
     attributes' intrinsic pixel size instead of the grid column width
     (reproducible even outside this page's own CSS) — wrapping in a block
     container sidesteps it. Same pattern epk.astro's .epk-photo-grid
     already uses for exactly this reason. Square (1:1), matching the
     reference's own appearance-card images exactly (measured ~242x242 at
     this viewport width) — not the 3:4 portrait used elsewhere on this
     page (.fv-media-frame), which is a different section. */
  .fv-appearance-image-frame {
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 14px;
  }

  .fv-appearance-image {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .fv-appearance-text {
    padding: 14px 2px 0;
  }

  /* Bold orange labels, always (not a hover-only accent) — matches the
     reference exactly. */
  .fv-appearance-text h3 {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 600;
    font-size: 1.15rem;
    letter-spacing: 0.02em;
    margin: 0 0 8px 0;
    color: var(--accent);
  }

  .fv-appearance-text p {
    font-size: 0.88rem;
    line-height: 1.6;
    color: rgba(255, 255, 255, 0.75);
    margin: 0;
    font-weight: 300;
  }

  /* SOUND & ATMOSPHERE: rounded containers for both the pre-click facade
     and the real iframe once injected (see the click-to-load script) —
     same radius/overflow treatment either way, so there's no visible
     shift in shape when the embed loads in. */
  .fv-embed-frame {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 20px;
  }

  .fv-embed-injected {
    border: none;
  }

  .fv-embed-injected-video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .fv-embed-injected-audio {
    width: 100%;
    display: block;
    border-radius: 12px;
    margin-bottom: 20px;
  }

  .fv-embed-facade {
    position: relative;
    display: block;
    width: 100%;
    border: none;
    padding: 0;
    margin: 0;
    cursor: pointer;
    background: #141414;
    overflow: hidden;
  }

  .fv-embed-video .fv-embed-facade {
    position: absolute;
    inset: 0;
    height: 100%;
  }

  .fv-embed-facade-audio {
    height: 400px;
    border-radius: 12px;
    margin-bottom: 20px;
  }

  .fv-embed-facade img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .fv-embed-play {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 68px;
    height: 68px;
    border-radius: 50%;
    background: rgba(0, 0, 0, 0.55);
    border: 1px solid rgba(255, 255, 255, 0.4);
    display: flex;
    align-items: center;
    justify-content: center;
    transition: border-color 0.3s ease, background 0.3s ease;
  }

  .fv-embed-play::after {
    content: '';
    width: 0;
    height: 0;
    border-style: solid;
    border-width: 12px 0 12px 20px;
    border-color: transparent transparent transparent #fff;
    margin-left: 4px;
    transition: border-color 0.3s ease;
  }

  .fv-embed-facade:hover .fv-embed-play,
  .fv-embed-facade:focus-visible .fv-embed-play {
    border-color: var(--accent);
    background: rgba(0, 0, 0, 0.7);
  }

  .fv-embed-facade:hover .fv-embed-play::after,
  .fv-embed-facade:focus-visible .fv-embed-play::after {
    border-left-color: var(--accent);
  }

  /* PRESS PHOTOS */
  .fv-photo-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 14px;
    margin: 20px 0;
  }

  /* aspect-ratio on the wrapping frame, not the <img> — see the matching
     comment on .fv-appearance-image-frame above for why. */
  .fv-photo-frame {
    aspect-ratio: 1 / 1;
    overflow: hidden;
    border-radius: 16px;
  }

  .fv-photo-grid img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .fv-download-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 16px;
    margin-top: 24px;
  }

  /* Press Assets buttons: solid-orange + outline-orange, matching the
     reference exactly (its "Download Press Kit" is filled, "Download
     Full Artist EPK" is outlined) — a moderately rounded rect, not a
     full pill, per the reference's own ~14px radius. */
  .cta-button {
    display: inline-block;
    font-family: 'Cormorant Garamond', serif;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    font-size: 0.85rem;
    text-decoration: none;
    padding: 14px 28px;
    border-radius: 14px;
    border: 1px solid var(--accent);
    transition: opacity 0.3s ease, background 0.3s ease;
  }

  .cta-button-solid {
    background: var(--accent);
    color: #000;
  }

  .cta-button-solid:hover,
  .cta-button-solid:focus-visible {
    opacity: 0.85;
  }

  .cta-button-outline {
    background: transparent;
    color: var(--accent);
  }

  .cta-button-outline:hover,
  .cta-button-outline:focus-visible {
    background: rgba(252, 131, 55, 0.12);
  }

  /* DISCOVER: bespoke markup rather than the shared SocialNav component,
     since this row needs a per-platform badge/line/label structure that
     SocialNav has no slot for — deliberately not touching that shared
     component just for one page's brief. Matches the reference: a
     horizontal line with each icon in a rounded-square badge sitting on
     it, a short drop line, and a brand-colored label beneath. Left-
     aligned heading, matching the reference (every other .fv-section h2
     is already left-aligned by default — this rule just removes the
     centering this section previously had). --brand-color is set inline
     per link (see the markup) so one set of rules covers all four. */
  .fv-discover-row {
    position: relative;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    flex-wrap: wrap;
    row-gap: 28px;
  }

  .fv-discover-line {
    position: absolute;
    top: 22px;
    left: 0;
    right: 0;
    height: 1px;
    background: rgba(255, 255, 255, 0.25);
  }

  .fv-discover-item {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-decoration: none;
  }

  .fv-discover-badge {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: #030303;
    border: 2px solid var(--brand-color);
    color: #fff;
    font-size: 1.15rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .fv-discover-drop {
    width: 1px;
    height: 20px;
    background: rgba(255, 255, 255, 0.3);
    margin: 2px 0;
  }

  .fv-discover-label {
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 0.02em;
    text-decoration: underline;
    color: var(--brand-color);
    white-space: nowrap;
  }

  .fv-discover-item:hover .fv-discover-badge,
  .fv-discover-item:focus-visible .fv-discover-badge {
    transform: translateY(-2px);
    box-shadow: 0 0 14px var(--brand-color);
  }

  /* BOOKING: left-aligned inner card (subtly lighter than the outer
     section fill), plain inline contact links rather than pill buttons,
     a footer strip with the beepd logo — matches the reference's actual
     structure, which is flatter than a button-CTA pattern. */
  .fv-booking-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    overflow: hidden;
    text-align: left;
  }

  .fv-booking-content {
    padding: 28px 30px;
  }

  .fv-booking-heading {
    font-weight: 600;
    font-size: 1.1rem;
    color: #fff !important;
    margin: 0 0 6px 0;
  }

  .fv-booking-name {
    font-family: 'Cormorant Garamond', serif;
    font-weight: 600;
    font-size: 1rem;
    color: rgba(255, 255, 255, 0.85) !important;
    margin: 0 0 16px 0;
  }

  .fv-booking-contact {
    font-size: 0.95rem !important;
    color: rgba(255, 255, 255, 0.85) !important;
    margin: 0 0 6px 0 !important;
  }

  .fv-booking-link {
    color: rgba(255, 255, 255, 0.92);
    text-decoration: underline;
    transition: color 0.3s ease;
  }

  .fv-booking-link:hover,
  .fv-booking-link:focus-visible {
    color: #fff;
  }

  .fv-booking-link-accent {
    color: var(--accent);
  }

  .fv-booking-link-accent:hover,
  .fv-booking-link-accent:focus-visible {
    color: #fff;
  }

  .fv-booking-note {
    font-size: 0.85rem !important;
    color: rgba(255, 255, 255, 0.55) !important;
    margin-top: 16px !important;
  }

  .fv-booking-footer {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 16px 30px;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    background: rgba(255, 255, 255, 0.02);
  }

  .fv-booking-footer img {
    display: block;
    height: 22px;
    width: auto;
  }

  /* MOBILE */
  @media (max-width: 860px) {
    .fv {
      padding: 110px 5% 70px;
      gap: 32px;
    }

    .fv-section {
      padding: 28px 22px;
      border-radius: 18px;
    }

    .fv-hero {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .fv-hero-portrait-frame {
      max-width: 240px;
      margin: 0 auto;
    }

    .fv-hero-content {
      text-align: center;
      align-items: center;
    }

    .fv-media,
    .fv-media-reverse {
      grid-template-columns: 1fr;
    }

    .fv-media-reverse .fv-media-frame {
      order: 0;
    }

    .fv-appearance-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 14px;
    }

    .fv-appearance-text {
      padding-top: 10px;
    }

    .fv-discover-row {
      justify-content: space-around;
      column-gap: 16px;
    }

    .fv-discover-line {
      display: none;
    }

    .fv-discover-badge {
      width: 40px;
      height: 40px;
    }

    .fv-discover-label {
      font-size: 0.75rem;
    }
  }

  @media (max-width: 560px) {
    .fv-photo-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
